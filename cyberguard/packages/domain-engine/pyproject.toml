[project]
name = "cyberguard-domain-engine"
version = "0.1.0"
description = "CyberGuard domain engine — automotive/medical/railway domain profiles and standards mapping"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["domain_engine"]
